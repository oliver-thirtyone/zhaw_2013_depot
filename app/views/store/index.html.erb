<h1>Store#index</h1>

<% @products.each do|product| %>
  <div class="entry">
    <h3><%= product.title %></h3>
    <p>Price: <%= product.price %></p>
    <p><%= product.description %></p>
    <%= image_tag(product.image_url) %>
  </div>
<% end %>
